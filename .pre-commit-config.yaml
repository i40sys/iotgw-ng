# Local layer-1 secret gate (task-067.04). Install once with `pre-commit install`.
# Pinned to gitleaks v8.30.1 to match the CI gate (.github/workflows/secret-scan.yml)
# and the same repo-root .gitleaks.toml allowlist (SOPS .enc.* ciphertext passes).
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
