[project]
name = "kestra-ansible-reporter"
version = "0.1.0"
description = "A tool to generate HTML reports from Kestra Ansible execution logs."
dependencies = [
    "requests",
    "ansi2html",
    "python-dotenv"
]

[project.scripts]
create-report = "reporter.main:main"
