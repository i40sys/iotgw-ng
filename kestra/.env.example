# .env.example — TEMPLATE (committed).
# Real values live encrypted in secrets/kestra.enc.env. Render with:
#   tools/secrets/secrets.sh render kestra
# Do NOT put real secrets in this file.
# Kestra stack secrets (was inline in kestra/docker-compose.yml + edge fn source)
KESTRA_POSTGRES_PASSWORD=CHANGEME
KESTRA_GEMINI_API_KEY=CHANGEME
KESTRA_BASIC_AUTH_USERNAME=CHANGEME
KESTRA_BASIC_AUTH_PASSWORD=CHANGEME
